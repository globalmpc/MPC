# Dedicated Pre CheckIn GA4 web stream; leave empty to disable analytics.
NEXT_PUBLIC_GA_MEASUREMENT_ID=
# Reown project with https://pre-check-in.globalmpc.tech in its origin allowlist.
# Without it, browser wallets remain available; WalletConnect is omitted.
NEXT_PUBLIC_REOWN_PROJECT_ID=
